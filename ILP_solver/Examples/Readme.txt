the files in this folder serve as an example to show how the program works on the files:
the input_data.json shows a valid input instance that will be processed by the program, the result of is the outputdata.json

the mapped files are intermediate representations of the data used inside the solver. They never exist normally as an actual file because they are python 
objects. 